// Placeholder until `pnpm gen:api` runs (requires backend dev server up at localhost:8001).
// openapi-typescript will overwrite this file with full path/operation types.
export type paths = Record<string, never>
