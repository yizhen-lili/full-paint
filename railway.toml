# 已棄用 — 改用 per-service 配置：
#   paint-web    → railway.web.toml
#   paint-worker → railway.worker.toml
#
# 各 service 在 Railway dashboard 設定：
#   Settings → Source → Config Path = railway.{web,worker}.toml
#
# 此檔保留為空殼，避免 Railway 預設讀到舊內容。
