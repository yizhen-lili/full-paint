// This file is a temporary placeholder. Run `pnpm gen:api` against the running
// FastAPI dev server (uvicorn at http://localhost:8000) to overwrite it with
// the real generated types.
//
//   pnpm gen:api
//
// After generation, the openapi-fetch client in `./client.ts` will be fully
// typed against every backend endpoint.

export type paths = Record<string, never>
